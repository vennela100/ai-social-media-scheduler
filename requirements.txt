# --- Core framework ---
Django>=5.2,<5.3

# --- Database ---
# psycopg3 is the modern driver; binary extra avoids needing local Postgres dev headers.
psycopg[binary]>=3.2
dj-database-url>=2.3          # parses DATABASE_URL into Django's DATABASES dict

# --- Config / secrets ---
python-dotenv>=1.0            # loads .env locally; no-op in prod where env vars are real

# --- Security ---
cryptography>=44.0           # symmetric encryption for OAuth tokens at rest

# --- Deployment (Render) ---
gunicorn>=23.0               # WSGI server used by Render
whitenoise>=6.8              # serves static files without a separate CDN

# --- Phase 1: media storage ---
cloudinary>=1.41             # video upload -> public URLs Instagram's API needs

# --- HTTP client (notifier now; Instagram/LinkedIn REST later) ---
requests>=2.32

# --- Email over HTTPS (Render free tier blocks outbound SMTP) ---
django-anymail>=10.0         # Brevo backend: send alert emails via HTTPS API

# --- Phase 2: AI metadata ---
google-genai>=1.0            # Gemini 2.5 Flash metadata generation

# --- Phase 3: YouTube OAuth + upload ---
google-api-python-client>=2.0   # videos.insert resumable upload
google-auth-oauthlib>=1.2       # OAuth 2.0 web flow
google-auth>=2.0                # credentials + token refresh
